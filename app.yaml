runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_KEY: base64:f7egGly9hfXA3nGGUBkeHs/YsFBg8uE+ppM5T4XsExY=
  DB_CONNECTION: mysql
  DB_HOST: 127.0.0.1
  DB_PORT: 3306
  DB_DATABASE: bobrovo_db
  DB_USERNAME: admin
  DB_PASSWORD: admin